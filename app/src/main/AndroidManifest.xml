<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vcc.vicente.mobitronics">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PatientList_Activity"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".NewPatient_Activity"
            android:parentActivityName=".PatientList_Activity" />
        <activity
            android:name=".TherapySettings_Activity"
            android:parentActivityName=".PatientList_Activity" />
        <activity
            android:name=".ChannelSettings_Activity"
            android:parentActivityName=".TherapySettings_Activity" />
        <activity
            android:name=".TherapyExecution_Activity"
            android:parentActivityName=".ChannelSettings_Activity" />
    </application>

</manifest>